import styles from "./hero.module.scss";

const Hero = () => {
  return (
    <section className={styles.hero}>
        <h1 className={styles.title}>Digital multi-tool with a wide skillset in programming, design and processes</h1>
    </section>
  );
};

export default Hero;